version: 2

sources:
  - name: million_songs
    database: "{{ env_var('GCP_PROJECT_ID') }}"
    schema: "{{ env_var('BQ_DATASET') }}"
    tables:
      - name: tracks
      - name: genres
      - name: lyrics
